spring:
  # 数据库配置（开发环境）
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://101.43.57.62:3306/kaipai_dev?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true&useSSL=false
    username: root
    password: root123456

  # Redis 配置（开发环境）
  data:
    redis:
      host: 101.43.57.62
      port: 6379
      password: kaipaileRedis888
      database: 0
      timeout: 10000ms

# 腾讯云 COS 配置（开发环境）
cos:
  secret-id: ${COS_SECRET_ID}
  secret-key: ${COS_SECRET_KEY}
  region: ap-shanghai
  bucket-name: kaipai-1412601014

# 日志配置（开发环境，开启 debug）
logging:
  level:
    com.kaipai: debug
    org.springframework.security: info

# MyBatis-Plus SQL 日志（开发环境打印 SQL）
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
